---
const { title } = Astro.props
---

<section>
  {title && <h2>{title}</h2>}
  <slot />
</section>

<style>
  section {
    max-width: 900px;
    margin: 0 auto 48px;
    padding: 0 1.5rem;
  }

  h2 {
    margin-bottom: 16px;
    font-weight: 700;
    line-height: 1.5;
    font-size: 2rem;
  }

  @media (max-width: 700px) {
    section {
      margin-bottom: 38px;
      padding: 0 1rem;
    }
  }
</style>
