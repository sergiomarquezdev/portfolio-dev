---
/**
 * TerminalHeader.astro - Barra de título de la terminal con botones de control
 */
import { MinimizeIcon, MaximizeIcon, CloseIcon } from '@/icons';

interface Props {
  title?: string;
  isMaximized?: boolean;
}

const {
  title = "Terminal",
  isMaximized = false
} = Astro.props;
---

<div class="terminal-header px-5 py-3 flex justify-between items-center" style="background-color: #374151;">
  <div class="text-sm font-medium" style="color: #ffffff;">
    {isMaximized ? `${title} (Maximizada)` : title}
  </div>
  <div class="flex space-x-3">
    <button
      id="terminal-minimize"
      class="w-6 h-6 rounded-full bg-yellow-500 hover:bg-yellow-400 transition-all hover:scale-105 hover:shadow-sm active:scale-95 focus:outline-none relative"
      title="Minimizar"
      aria-label="Minimizar terminal"
    >
      <MinimizeIcon class="w-4 h-4 m-auto text-yellow-800" />
    </button>
    <button
      id="terminal-maximize"
      class="w-6 h-6 rounded-full bg-green-500 hover:bg-green-400 transition-all hover:scale-105 hover:shadow-sm active:scale-95 focus:outline-none relative"
      title="Maximizar"
      aria-label="Maximizar terminal"
    >
      <MaximizeIcon class="w-4 h-4 m-auto text-green-800" />
    </button>
    <button
      id="terminal-close"
      class="w-6 h-6 rounded-full bg-red-500 hover:bg-red-400 transition-all hover:scale-105 hover:shadow-sm active:scale-95 focus:outline-none relative"
      title="Cerrar"
      aria-label="Cerrar terminal"
    >
      <CloseIcon class="w-4 h-4 m-auto text-red-800" />
    </button>
  </div>
</div>
