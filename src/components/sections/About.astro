---
import Section from "../Section.astro";
import { basics } from "@cv";

const { summaryIntro, summaryExperience, summaryObjectives } = basics;

const highlightKeywords = (text: string) => {
  const keywords = [
    "Java",
    "Spring Framework",
    "Python",
    "Go",
    "Docker",
    "Kubernetes",
    "Google Cloud",
    "PostgreSQL",
    "MongoDB",
    "Pinecone",
    "FastAPI",
    "n8n",
    "CI/CD",
    "LLMOps",
    "backend",
    "APIs",
    "inteligencia artificial",
    "machine learning",
  ];
  const regex = new RegExp(`\b(${keywords.join("|")})\b`, "gi");
  return text.replace(regex, `<strong class="text-primary font-semibold">$1</strong>`);
};

const contactButtonClasses =
  "inline-flex items-center gap-2 px-6 py-2 rounded-md bg-accent-cta text-text dark:text-text font-medium transition-all duration-200 hover:bg-accent-cta/80 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-accent-cta focus:ring-offset-2 focus:ring-offset-background-surface";
---

<Section title="Sobre mí" id="sobre-mi">
  <article class="bg-background-surface p-l rounded-lg border border-border shadow-lg">
    <div class="space-y-m">
      <div class="paragraph-container flex items-start gap-m mb-s">
        <div
          class="flex-shrink-0 w-16 h-16 bg-gradient-to-br from-primary to-secondary-blue rounded-full flex items-center justify-center text-white font-bold text-xl shadow-lg"
        >
          SM
        </div>
        <div class="flex-grow">
          <h3 class="text-h4 font-semibold text-text mb-2">¡Hola! 👋</h3>
          <p
            class="text-body-lg text-text-muted leading-relaxed"
            set:html={highlightKeywords(summaryIntro)}
          />
        </div>
      </div>

      <div class="paragraph-container">
        <div class="border-l-4 border-secondary-blue pl-m">
          <h4 class="text-body-lg font-semibold text-text mb-s flex items-center gap-2">
            <svg class="w-5 h-5 text-secondary-blue" fill="currentColor" viewBox="0 0 20 20">
              <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            Experiencia Técnica
          </h4>
          <p
            class="text-body-reg text-text-muted leading-relaxed"
            set:html={highlightKeywords(summaryExperience)}
          />
        </div>
      </div>

      <div class="paragraph-container">
        <div
          class="bg-gradient-to-r from-secondary-purple/10 to-accent-pink/5 p-m rounded-lg border border-secondary-purple/20"
        >
          <h4 class="text-body-lg font-semibold text-text mb-s flex items-center gap-2">
            <svg class="w-5 h-5 text-secondary-purple" fill="currentColor" viewBox="0 0 20 20">
              <path
                fill-rule="evenodd"
                d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-8.293l-3-3a1 1 0 00-1.414 0l-3 3a1 1 0 001.414 1.414L9 9.414V13a1 1 0 102 0V9.414l1.293 1.293a1 1 0 001.414-1.414z"
                clip-rule="evenodd"></path>
            </svg>
            Objetivos Profesionales
          </h4>
          <p
            class="text-body-reg text-text-muted leading-relaxed"
            set:html={highlightKeywords(summaryObjectives)}
          />
        </div>
      </div>
    </div>

    <div class="mt-l pt-m border-t border-border text-center">
      <p class="text-sm text-text-muted mb-s">¿Interesado en colaborar?</p>
      <a href="#contacto" class:list={[contactButtonClasses]}>
        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
          <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"></path>
          <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"></path>
        </svg>
        Hablemos
      </a>
    </div>
  </article>
</Section>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const observer = new IntersectionObserver(
      (entries, obs) => {
        entries.forEach((entry, index) => {
          if (entry.isIntersecting) {
            const el = entry.target as HTMLElement;
            el.style.setProperty("--animation-delay", `${index * 200}ms`);
            el.classList.add("animate-fade-in-up");
            obs.unobserve(el);
          }
        });
      },
      { threshold: 0.1 }
    );

    document.querySelectorAll(".paragraph-container").forEach((p) => observer.observe(p));
  });
</script>
